#pragma once
#include "UILayerBase.h"

class UILayer_sGame_TimeAttack_Start : public UILayerBase {
public:
	UILayer_sGame_TimeAttack_Start(void) = default;
	~UILayer_sGame_TimeAttack_Start(void) override = default;

private:

	void SetDefaultUI(void) override;
};

